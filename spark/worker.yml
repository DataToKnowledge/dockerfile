worker:
    extends:
        file: base.yml
        service: spark
    command: /spark/bin/spark-class org.apache.spark.deploy.worker.Worker spark://spark-master:7077
    ports:
        - 8081:8081
    volumes:
        - ./conf/worker:/conf
